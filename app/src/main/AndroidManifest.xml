<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.envisionate.musicexplorer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <meta-data
        android:name="com.google.android.gms.car.application"
        android:resource="@xml/automotive_app_desc" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/track"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicExplorer">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="8"/>

        <activity
            android:name="com.envisionate.musicexplorer.MusicExplorer"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.envisionate.musicexplorer.Settings"
            android:exported="false"
            android:launchMode="singleTop"
            android:label="@string/action_settings">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.envisionate.musicexplorer.Play"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/action_play">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.envisionate.carservice.CarService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService"/>
                <category android:name="androidx.car.app.category.POI"/>
            </intent-filter>
        </service>

        <receiver
            android:name="com.envisionate.musicexplorer.MusicExplorerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.envisionate.musicexplorer.START" />
                <action android:name="com.envisionate.musicexplorer.STARTED" />
                <action android:name="com.envisionate.musicexplorer.STARTED_WITH_PLAY" />
                <action android:name="com.envisionate.musicexplorer.SETUP_NOTIFY"/>
                <action android:name="com.envisionate.musicexplorer.SETUP_DONE"/>
                <action android:name="com.envisionate.musicexplorer.PLAY_NOTIFY"/>
                <action android:name="com.envisionate.musicexplorer.NAVIGATION_NOTIFY"/>
                <action android:name="com.envisionate.musicexplorer.STOP_REQUESTED"/>
                <action android:name="com.envisionate.musicexplorer.CAR_PLAY_STOPPED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>